general {
    col.active_border = rgba(93909177)
    col.inactive_border = rgba(48464855)
}

misc {
    background_color = rgba(141314FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = SF Pro, Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(141314FF)
        col.text = rgba(e6e1e3FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(e6e1e3), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(e6e1e3), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(e6e1e3), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrule = border_color rgba(d0c2d3AA) rgba(d0c2d377), match:pin 1
