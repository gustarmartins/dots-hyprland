general {
    col.active_border = rgba(8b919877)
    col.inactive_border = rgba(41484d55)
}

misc {
    background_color = rgba(101417FF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = SF Pro, Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(101417FF)
        col.text = rgba(dfe3e7FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(dfe3e7), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(dfe3e7), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(dfe3e7), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrule = border_color rgba(92cef6AA) rgba(92cef677), match:pin 1
