# Automatic idle actions are intentionally disabled.
#
# Keep this file inert so an accidental manual hypridle launch cannot lock the
# session, switch off the displays, or suspend the system. Manual Super+L and
# Super+Shift+L remain configured in Hyprland.
