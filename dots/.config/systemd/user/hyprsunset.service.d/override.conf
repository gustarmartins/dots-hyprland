[Service]
# The actual neutral/default transform is identity, not hyprsunset's 6000 K
# default matrix.  End-4 applies the user's requested temperature afterward.
ExecStart=
ExecStart=/usr/bin/hyprsunset --identity
Restart=always
RestartSec=1s
